interface:
  display_name: "Codex Process Hygiene"
  short_description: "Best-effort checkpoint cleanup skill for temporary development processes across mainstream languages and browser automation stacks"
default_prompt: "When coding work may leave temporary processes behind across JavaScript, Rust, Python, JVM or .NET, Go, Ruby, PHP, Elixir, or browser automation stacks, use $codex-cleaning-temporary-processes through best-effort implicit invocation at finished checkpoints such as high-risk tool steps, finished build or test steps, browser automation or remote-debug steps, completed one-shot command batches, or subagent completion; cover tools such as npm, bun, cargo, tauri, trunk, hatch, pytest, jupyter, streamlit, kotlin, scala, dotnet, bundle, composer, mix, and chrome-devtools-mcp; preserve the active Codex shell, keep reusable dev servers unless the checkpoint is clearly final, require current-task lineage or confirmed current-thread ownership for explicit automation, never let workspace match alone authorize explicit automation cleanup, keep generic runtimes workspace-scoped, and preserve multi-project isolation."
policy:
  allow_implicit_invocation: true
